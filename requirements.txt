# Runtime dependencies for the cmc package and the notebooks.
# Deliberately loose for now -- pin once the results are being written up.
#
#   pip install -r requirements.txt
#   pip install -e .
#
# Note on torch: this installs the default wheel for your platform, which on
# Linux is a CUDA build and on Windows/macOS is CPU-only. For a specific CUDA
# version, install torch first from https://pytorch.org/get-started/locally/
# and then run this file -- the pin below is already satisfied and won't be
# replaced.

# --- core: training and Pareto sweeps -------------------------------------
torch>=2.0
numpy>=1.24
matplotlib>=3.7
tqdm>=4.65

# --- notebooks: analysis and plotting -------------------------------------
pandas>=2.0
scipy>=1.10
scikit-learn>=1.3
plotly>=5.18
jupyterlab>=4.0
ipykernel>=6.25

# --- planned: multi-objective optimisation over the front -----------------
pymoo>=0.6.2
